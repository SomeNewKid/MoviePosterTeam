schema_version = 1
agent_id = "poster_agent"
module = "poster_agent"

container_capabilities = [
  "network",
  "shared_volume",
]

application_capabilities = [
  "a2a",
  "image_artifacts",
  "mcp_client",
]

[squid_proxy]
allowed_domains = []
allowed_ip_addresses = []

[mcp_sidecar]
tools = [
  "generate_image",
]
resources = []
